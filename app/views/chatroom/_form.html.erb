<div class="form-group px-3"> 
  <%= form_for( :message, remote: true)  do |form| %>
     <div class="field" >
      <%= form.text_field :body,id:"form_message" %>
    </div>

    <div class="actions" >
      <%= form.submit %>
    </div>
  <% end %>
</div>
