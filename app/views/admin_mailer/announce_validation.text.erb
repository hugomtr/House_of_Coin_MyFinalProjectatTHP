New Announcement
==========================================================

Announcement n° <%= @real_estate_id %>
    <% user_id = @real_estate.user_id %>
    <% real_estate_id = @real_estate.real_estate_id %>
    Seller :      <%= User.find(user_id).email %>
    Price :       <%= real_estate_price = @real_estate.price %>
    Adress :      <%= real_estate_adress = @real_estate.adress %>
    Zipcode :     <%= real_estate_zipcode = @real_estate.zipcode %>
    City :        <%= real_estate_city = @real_estate.city %>
    Description : <%= real_estate_description = @real_estate.description %>